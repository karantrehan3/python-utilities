services:
  python-utilities-api:
    build: .
    ports:
      - "4001:4001"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      # Mount a volume for temporary files if needed
      - /tmp:/tmp
    restart: unless-stopped
