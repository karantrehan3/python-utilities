import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'blue',
  fontFamily: "'SF Mono', 'Cascadia Code', 'JetBrains Mono', ui-monospace, 'Fira Code', monospace",
  headings: {
    fontFamily: "system-ui, 'Segoe UI', Roboto, sans-serif",
  },
  defaultRadius: 'md',
});
